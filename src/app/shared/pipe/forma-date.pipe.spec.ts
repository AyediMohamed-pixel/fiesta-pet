import { FormaDatePipe } from './forma-date.pipe';

describe('FormaDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormaDatePipe();
    expect(pipe).toBeTruthy();
  });
});
